package cn.edu.sau.url;

public class UrlString {

    //IP地址
    private static final String IP = "192.168.43.248";

  //  private static final String IP = "202.112.30.6";

    // 基地址
    public static final String BASE_URL = "http://" + IP
            + "/Android";

    // 登录
    public static final String Login = "http://" + IP
            + "/Android/Login";

    // 管理员登陆
    public static final String adminLogin = "http://" + IP
            + "/Android/AdminLogin";

    // 查询工作人员列表
    public static final String queryWorkerList = "http://" + IP
            + "/Android/QueryWorkerList";

    // 添加工作人员到列表
    public static final String addWorkerToList = "http://" + IP
            + "/Android/AddWorkerToList";

    // 更新工作人员列表
    public static final String updateWokerList = "http://" + IP
            + "/Android/UpdateWorkerList";

    // 删除某个工作人员
    public static final String delWorkerFromList = "http://" + IP
            + "/Android/DelWorkerFromList";

    // 上传图片
    public static final String uploadImage = "http://" + IP
            + "/Android/UploadImage";

    // 上传图片
    public static final String addHouseToList = "http://" + IP
            + "/Android/AddHouseToList";

    // 查询房屋列表
    public static final String queryHouseList = "http://" + IP
            + "/Android/QueryHouseList";

    // 房源信息统计
    public static final String queryHouseStatistics = "http://" + IP
            + "/Android/QueryHouseStatistics";

    // 添加房主信息
    public static final String addOwnerToList = "http://" + IP
            + "/Android/AddOwnerToList";

    // 添加房客信息
    public static final String addTenantToList = "http://" + IP
            + "/Android/AddTDemandToList";

    // 添加合同信息
    public static final String addContractToList = "http://" + IP
            + "/Android/AddContractToList";

    // 查询合同列表
    public static final String queryContractList = "http://" + IP
            + "/Android/QueryContractList";

    // 查询合同统计信息
    public static final String queryContractStatistics = "http://" + IP
            + "/Android/QueryContractStatistics";

    // 查询房主列表
    public static final String queryOwnerList = "http://" + IP
            + "/Android/QueryOwnerList";

    // 查询房客列表
    public static final String queryTenantList = "http://" + IP
            + "/Android/QueryTenantList";

    // 查询房屋详细信息
    public static final String queryHouseOne = "http://" + IP
            + "/Android/QueryHouseOne";

    // 查询房主详细信息
    public static final String queryOwnerOne = "http://" + IP
            + "/Android/QueryOwnerOne";

    // 查询租客信息
    public static final String queryTenantOne = "http://" + IP
            + "/Android/QueryTenantOne";

    // 查询合同的详细信息
    public static final String queryContractOne = "http://" + IP
            + "/Android/QueryContractOne";

    // 更新房屋信息
    public static final String updateHouseOne = "http://" + IP
            + "/Android/UpdateHouseOne";

    // 删除房屋信息
    public static final String deleteHouseOne = "http://" + IP
            + "/Android/DeleteHouseOne";

    // 更新合同信息
    public static final String updateContract = "http://" + IP
            + "/Android/UpdateContract";

    // 删除合同信息
    public static final String deleteContract = "http://" + IP
            + "/Android/DeleteContract";

    // 更新房主信息
    public static final String updateOwner = "http://" + IP
            + "/Android/UpdateOwner";

    // 删除房主信息
    public static final String deleteOwner = "http://" + IP
            + "/Android/DeleteOwner";

    // 更新房客信息
    public static final String updateTenant = "http://" + IP
            + "/Android/UpdateTenant";

    // 删除房客信息
    public static final String deleteTenant = "http://" + IP
            + "/Android/DeleteTenant";
}